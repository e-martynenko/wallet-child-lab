# Wallet Child Lab does not automatically load .env files.
# Export these variables explicitly before running read-only network checks.
WALLET_CHILD_NETWORK=devnet
WALLET_CHILD_RPC_URL=https://api.devnet.solana.com
